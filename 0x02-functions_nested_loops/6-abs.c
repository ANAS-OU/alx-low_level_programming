/**
 * _abs - function that computes the absolute value of
 * an integer.
 * @n: function param.
 *
 * Return: absolute value.
 */

int _abs(int n)
{
	if (n < 0)
		return (-1 * n);
	else
		return (n);
}
